<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="azuwaredison89.gmail.dinacom">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".ui.activity.pesandokter.HargaDokterActivity" />

        <activity android:name=".ui.activity.pesanperawat.DaftarNamaPerawatActivity" />
        <activity android:name=".ui.activity.pesandokter.DaftarNamaDokterActivity" />
        <activity android:name=".ui.activity.pesanperawat.DataPerawatActivity" />

        <activity android:name=".puskesmas.PerawatActivity" />
        <activity android:name=".ui.activity.pesandokter.DokterActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.pesandokter.DataDokterActivity" />
        <activity android:name=".ui.activity.home.DashboardActivity" />
    </application>

</manifest>